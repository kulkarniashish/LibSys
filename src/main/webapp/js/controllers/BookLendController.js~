angular.module('libSysApp').controller('BookLendController', 
			function($scope, $http, $routeParams, $location) {


	var id = $routeParams.id;
	var userId = $routeParams.userId;

	if (!id) {
		$scope.bookLend = {
			"userId" : $routeParams.userId
		};
		return;
	}
	
}); 
